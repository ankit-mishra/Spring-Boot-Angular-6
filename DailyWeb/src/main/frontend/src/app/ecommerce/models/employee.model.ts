export class Employee {
    id: number;
    name: string;
    price: number;
    activity: string;

    constructor(id: number, name: string, price: number, activity: string) {
        this.id = id;
        this.name = name;
        this.price = price;
        this.activity = activity;
    }
}
